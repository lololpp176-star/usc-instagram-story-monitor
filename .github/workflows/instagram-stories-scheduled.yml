name: Instagram Stories Automatic Monitor (retired)

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Use the bounded Instagram Stories via Instaloader workflow."
